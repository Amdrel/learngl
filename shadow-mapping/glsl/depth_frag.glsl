#version 330 core

void main() {
  // A fragment shader is not needed for creating a depth map but still
  // required by OpenGL. Simply do nothing as the depth will be automatically
  // set by any sane OpenGL implementation.
}
